import * as THREE from 'three';const MAX=3000;
export class Particles{mesh:THREE.Points;p=new Float32Array(MAX*3);v=new Float32Array(MAX*3);c=new Float32Array(MAX*3);life=new Float32Array(MAX);cursor=0;constructor(scene:THREE.Scene){const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.BufferAttribute(this.p,3));g.setAttribute('color',new THREE.BufferAttribute(this.c,3));this.mesh=new THREE.Points(g,new THREE.PointsMaterial({size:.16,vertexColors:true,transparent:true,opacity:.9,depthWrite:false,blending:THREE.AdditiveBlending}));scene.add(this.mesh)}emit(x:number,y:number,color:number,count:number,speed=3){const col=new THREE.Color(color);for(let n=0;n<count;n++){const i=this.cursor++%MAX,j=i*3,a=Math.random()*Math.PI*2,s=speed*(.3+Math.random()*.7);this.p[j]=x;this.p[j+1]=y;this.p[j+2]=1;this.v[j]=Math.cos(a)*s;this.v[j+1]=Math.abs(Math.sin(a))*s;this.c[j]=col.r;this.c[j+1]=col.g;this.c[j+2]=col.b;this.life[i]=.35+Math.random()*.8}}update(dt:number){for(let i=0;i<MAX;i++){if(this.life[i]<=0)continue;const j=i*3;this.life[i]-=dt;this.v[j+1]-=8*dt;this.p[j]+=this.v[j]*dt;this.p[j+1]+=this.v[j+1]*dt;if(this.life[i]<=0)this.p[j+1]=-999}(this.mesh.geometry.getAttribute('position') as THREE.BufferAttribute).needsUpdate=true;(this.mesh.geometry.getAttribute('color') as THREE.BufferAttribute).needsUpdate=true}}

